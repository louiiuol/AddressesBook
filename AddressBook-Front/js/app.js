let loader = document.getElementById('loader');
loader.onclick = () => {
    alert("sending order: HOLD THE DOOR !");
    // Envoie requete http get à  l'adresses ci dessous 
    // http://localhost:8082/addressesBook/loader/
};